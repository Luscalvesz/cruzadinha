function memoria() {
    var word = document.querySelectorAll(".memo");
    var i
    var resp = document.getElementById('in_memoria').value
    if (resp == 'memoria de massa' || resp == 'memória de massa' ) {
        for (i = 0; i < word.length; i++) {
            word[i].classList.add('ativo')
          }
    }else{
        alert('Errado! Tente novamente.')
    }
}
function cpu() {
    var word = document.querySelectorAll(".cpu");
    var i
    var resp = document.getElementById('in_cpu').value
    if (resp == 'cpu') {
        for (i = 0; i < word.length; i++) {
            word[i].classList.add('ativo', 'opacityOn')
          }
    }else{
        alert('Errado! Tente novamente.')
    }
}
function ula() {
    var word = document.querySelectorAll(".ula");
    var i
    var resp = document.getElementById('in_ula').value
    if (resp == 'ula') {
        for (i = 0; i < word.length; i++) {
            word[i].classList.add('ativo', 'opacityOn')
          }
    }else{
        alert('Errado! Tente novamente.')
    }
}
function regist() {
    var word = document.querySelectorAll(".regist");
    var i
    var resp = document.getElementById('in_regist').value
    if (resp == 'registradores') {
        for (i = 0; i < word.length; i++) {
            word[i].classList.add('ativo', 'opacityOn')
          }
    }else{
        alert('Errado! Tente novamente.')
    }
}
function ram() {
    var word = document.querySelectorAll(".ram");
    var i
    var resp = document.getElementById('in_ram').value
    if (resp == 'ram') {
        for (i = 0; i < word.length; i++) {
            word[i].classList.add('ativo', 'opacityOn')
          }
    }else{
        alert('Errado! Tente novamente.')
    }
}
function rom() {
    var word = document.querySelectorAll(".rom");
    var i
    var resp = document.getElementById('in_rom').value
    if (resp == 'rom') {
        for (i = 0; i < word.length; i++) {
            word[i].classList.add('ativo', 'opacityOn')
          }
    }else{
        alert('Errado! Tente novamente.')
    }
}
function eprom() {
    var word = document.querySelectorAll(".eprom");
    var i
    var resp = document.getElementById('in_eprom').value
    if (resp == 'eprom') {
        for (i = 0; i < word.length; i++) {
            word[i].classList.add('ativo', 'opacityOn')
          }
    }else{
        alert('Errado! Tente novamente.')
    }
}
function flash() {
    var word = document.querySelectorAll(".flash");
    var i
    var resp = document.getElementById('in_flash').value
    if (resp == 'flash') {
        for (i = 0; i < word.length; i++) {
            word[i].classList.add('ativo', 'opacityOn')
          }
    }else{
        alert('Errado! Tente novamente.')
    }
}
function dma() {
    var word = document.querySelectorAll(".dma");
    var i
    var resp = document.getElementById('in_dma').value
    if (resp == 'dma') {
        for (i = 0; i < word.length; i++) {
            word[i].classList.add('ativo', 'opacityOn')
          }
    }else{
        alert('Errado! Tente novamente.')
    }
}
function cs() {
    var word = document.querySelectorAll(".cs");
    var i
    var resp = document.getElementById('in_cs').value
    if (resp == 'cs') {
        for (i = 0; i < word.length; i++) {
            word[i].classList.add('ativo', 'opacityOn')
          }
    }else{
        alert('Errado! Tente novamente.')
    }
}
function adress() {
    var word = document.querySelectorAll(".adress");
    var i
    var resp = document.getElementById('in_adress').value
    if (resp == 'adress bus') {
        for (i = 0; i < word.length; i++) {
            word[i].classList.add('ativo', 'opacityOn')
          }
    }else{
        alert('Errado! Tente novamente.')
    }
}
function data() {
    var word = document.querySelectorAll(".data");
    var i
    var resp = document.getElementById('in_data').value
    if (resp == 'data bus') {
        for (i = 0; i < word.length; i++) {
            word[i].classList.add('ativo', 'opacityOn')
          }
    }else{
        alert('Errado! Tente novamente.')
    }
}
function i5() {
    var word = document.querySelectorAll(".i5");
    var i
    var resp = document.getElementById('in_i5').value
    if (resp == 'i5') {
        for (i = 0; i < word.length; i++) {
            word[i].classList.add('ativo', 'opacityOn')
          }
    }else{
        alert('Errado! Tente novamente.')
    }
}
function i7() {
    var word = document.querySelectorAll(".i7");
    var i
    var resp = document.getElementById('in_i7').value
    if (resp == 'i7') {
        for (i = 0; i < word.length; i++) {
            word[i].classList.add('ativo', 'opacityOn')
          }
    }else{
        alert('Errado! Tente novamente.')
    }
}
function dual() {
    var word = document.querySelectorAll(".dual");
    var i
    var resp = document.getElementById('in_dual').value
    if (resp == 'dual core') {
        for (i = 0; i < word.length; i++) {
            word[i].classList.add('ativo', 'opacityOn')
          }
    }else{
        alert('Errado! Tente novamente.')
    }
}
function quad() {
    var word = document.querySelectorAll(".quad");
    var i
    var resp = document.getElementById('in_quad').value
    if (resp == 'quad core') {
        for (i = 0; i < word.length; i++) {
            word[i].classList.add('ativo', 'opacityOn')
          }
    }else{
        alert('Errado! Tente novamente.')
    }
}





